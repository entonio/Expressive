//
// Copyright © 2024 Antonio Marques. All rights reserved.
//

import Foundation

/*
extension Expression: Codable {
    public func encode(to encoder: any Encoder) throws {
        try content.encode(to: encoder)
    }

    public init(from decoder: any Decoder) throws {
        if let content = try? decoder.singleValueContainer().decode(Int.self) {
            self.content = content
        } else if let content = try? decoder.singleValueContainer().decode(Double.self) {
            self.content = content
        } else if let content = try? decoder.singleValueContainer().decode(String.self) {
            self.content = content
        } else if let content = try? decoder.singleValueContainer().decode(Rational.self) {
            self.content = content
        } else if let content = try? decoder.singleValueContainer().decode(Tuple.self) {
            self.content = content
        }
        fatalError("Unable to decode Expression from [\(decoder)]")
    }* /
}
*/

